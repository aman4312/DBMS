Blob: Binary large object
      It is used to store large binary data
      Used to store videos , images.
Clob: character large object
      It is used to store character large data.
      Used to store text, word, pdf.
1: TO CREATE NEW TABLE FROM EXISTING TABLE:
USE: CREATE TABLE NEW_TABLE AS SELECT COLUMN_NAME FROM EXISTING_TABLE;

2. USERNAME SHOULD HAVE MINIMUM 3 CHARACTESRS
CREATE TABLE PROFILE(
USERNAME VARCHAR(24) CHECK( LENGTH(USERNAME)>=3),
FULL_NAME VARCHAR(72),
HEADLINE VARCHAR(72)

);

3. Second highest salary from table:
select max(sal) from emp where sal not in (select max(sal) from emp);
